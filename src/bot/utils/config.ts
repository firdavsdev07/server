import "dotenv/config";

const config = {
  BOT_TOKEN: process.env.BOT_TOKEN,
  BOT_USERNAME: process.env.BOT_USERNAME,
  BOT_WEB_APP_URL: process.env.BOT_WEB_APP_URL,
};

export default config;
